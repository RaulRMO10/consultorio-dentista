# ──────────────────────────────────────────────────────────────
# OdontoSystem — exemplo de variáveis de ambiente
# Copie este arquivo para .env e preencha com seus valores reais.
#   cp .env.example .env
# ──────────────────────────────────────────────────────────────

# Supabase REST API (obrigatório)
# Encontre em: https://supabase.com/dashboard/project/<id>/settings/api
SUPABASE_URL=https://<SEU_PROJECT_ID>.supabase.co
SUPABASE_KEY=<SUA_SUPABASE_ANON_KEY>

# Banco de dados PostgreSQL direto (usado pelo setup_database.py)
# Encontre em: https://supabase.com/dashboard/project/<id>/settings/database
DB_HOST=db.<SEU_PROJECT_ID>.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=<SUA_DB_PASSWORD>

# JWT — gere uma chave forte:  python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=<CHAVE_SECRETA_FORTE_AQUI>
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=8

# Aplicação
APP_NAME="OdontoSystem"
APP_VERSION=1.0.0
DEBUG=False

# API / Backend
API_HOST=localhost
API_PORT=8000
BACKEND_URL=http://localhost:8000

# Frontend
STREAMLIT_PORT=8501
